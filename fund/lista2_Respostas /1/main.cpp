#include <iostream>
using namespace std;

int main() {
    // Declaração das variáveis
    int x, y, z;

    // Cálculo de x
    x = 7 + 3 * 6 / 2 - 1;

    // Cálculo de y
    y = 2 % 2 + 2 * 2 / 2;

    // Cálculo de z
    z = (3 * 9 * (3 + (9 * 3 / (3))));

    // Impressão dos valores de x, y e z
    cout << "Valor de x: " << x << endl;
    cout << "Valor de y: " << y << endl;
    cout << "Valor de z: " << z << endl;

    return 0;
}
